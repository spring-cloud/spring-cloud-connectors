{
        "name": "$serviceName",
        "label": "mongolab-$version",
        "plan": "free",
        "tags":["mongodb"],
        "credentials": {
            "uri" : "mongodb://$username:$password@$hostname:$port/$db"
        }
}
